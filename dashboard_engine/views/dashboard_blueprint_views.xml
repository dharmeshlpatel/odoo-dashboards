<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_dashboard_blueprint_tree" model="ir.ui.view">
        <field name="name">dashboard.blueprint.list</field>
        <field name="model">dashboard.blueprint</field>
        <field name="arch" type="xml">
            <list js_class="dashboard_blueprint_list"
                  action="action_open_studio"
                  type="object"
                  sample="1">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="host_model_id" string="Host Model"/>
                <field name="state" decoration-success="state == 'published'"
                       decoration-muted="state == 'draft'" widget="badge"/>
                <field name="health_issue_count" string="Issues"
                       decoration-danger="health_issue_count &gt; 0"
                       decoration-success="health_issue_count == 0"
                       optional="show"/>
                <field name="module_ids" widget="many2many_tags" string="Required Apps"
                       optional="show"/>
                <field name="menu_name" string="Menu Name" optional="hide"/>
                <field name="key" string="Key" optional="hide"/>
                <field name="company_id" optional="hide" groups="base.group_multi_company"/>
                <button name="action_open_studio" type="object" string="Customize"
                        icon="fa-pencil"
                        groups="dashboard_engine.group_dashboard_engine_studio"/>
                <button name="action_open_advanced" type="object" string="Advanced"
                        icon="fa-flask"
                        groups="dashboard_engine.group_dashboard_engine_manager"/>
            </list>
        </field>
    </record>

    <record id="view_dashboard_blueprint_search" model="ir.ui.view">
        <field name="name">dashboard.blueprint.search</field>
        <field name="model">dashboard.blueprint</field>
        <field name="arch" type="xml">
            <search string="Dashboards">
                <field name="name"/>
                <field name="key" string="Key"/>
                <field name="host_model_id" string="Host Model"/>
                <field name="menu_name" string="Menu Name"/>
                <field name="company_id" groups="base.group_multi_company"/>
                <filter string="Published" name="published"
                        domain="[('state', '=', 'published')]"/>
                <filter string="Draft" name="draft"
                        domain="[('state', '=', 'draft')]"/>
                <separator/>
                <filter string="Archived" name="inactive"
                        domain="[('active', '=', False)]"/>
                <group>
                    <filter string="State" name="group_state"
                            context="{'group_by': 'state'}"/>
                    <filter string="Host Model" name="group_host"
                            context="{'group_by': 'host_model_id'}"/>
                    <filter string="Company" name="group_company"
                            context="{'group_by': 'company_id'}"
                            groups="base.group_multi_company"/>
                </group>
            </search>
        </field>
    </record>

    <!--
      The settings popup every generated dashboard opens from its gear.
      One form serves all blueprints: the choices on offer are narrowed by
      the blueprint the preference belongs to rather than by the arch.
    -->
    <record id="dashboard_user_pref_view_form" model="ir.ui.view">
        <field name="name">dashboard.user.pref.form</field>
        <field name="model">dashboard.user.pref</field>
        <field name="arch" type="xml">
            <!--
              Shared settings popup for every blueprint. Labels/help for
              scopes come from dashboard.blueprint.scope (+ label variants);
              date columns light up from the pref's period_* fields (seeded
              from the blueprint). No host-app strings live in this view.
            -->
            <form string="Configuration"
                  class="oe_form_configuration o_base_settings o_dashboard_engine_settings">
                <field name="graph_model" invisible="1"/>
                <field name="graph_model_id" invisible="1"/>
                <field name="blueprint_id" invisible="1"/>
                <field name="scope_warning" invisible="1"/>
                <field name="has_include_scope" invisible="1"/>
                <field name="has_restrict_scope" invisible="1"/>
                <field name="applicable_include_scope_ids" invisible="1"/>
                <field name="groupby_has_date" invisible="1"/>
                <field name="ordered_groupby_ids" invisible="1"/>
                <!-- Legacy Open/Closed field ids stay mirrored from period lines. -->
                <field name="period_field_id" invisible="1"/>
                <field name="period_closed_field_id" invisible="1"/>
                <field name="has_period_lines" invisible="1"/>
                <field name="preferred_graph_model" invisible="1"/>
                <field name="has_graph_variants" invisible="1"/>
                <sheet>
                    <!-- 1) My Data (restrict scopes) — most used personal switch -->
                    <div class="o_setting_container" invisible="not has_restrict_scope">
                        <h4><b>My Data</b></h4>
                        <div class="o_dashboard_engine_setting_help">
                            Start here if you only want to see work assigned to you.
                            Turn it on to hide other people's records from the chart,
                            KPIs, bottom buttons, views, and reports (including linked
                            Sales when a map is active). Leave it off to see the full team
                            picture.
                        </div>
                        <field name="scope_ids" nolabel="1" colspan="2"
                               widget="dashboard_scope_checkboxes"
                               domain="[('blueprint_id', '=', blueprint_id),
                                        ('mode', '=', 'restrict')]"/>
                    </div>

                    <!-- 2) Graph Configuration: Chart model → Group By → Measures → Data to Include -->
                    <div class="o_setting_container">
                        <h4><b>Graph Configuration</b></h4>
                        <div class="o_dashboard_engine_setting_help">
                            Use this section to build the chart. Everything below
                            (Chart Model, Group By, Measures, and Data to Include)
                            belongs here and changes the chart only — not KPIs,
                            bottom buttons, views, or reports.
                        </div>
                        <div class="o_dashboard_engine_subsections">
                            <!-- Chart model first so users see the picker when variants exist -->
                            <div class="row o_settings_container" invisible="not has_graph_variants">
                                <div class="col-12 o_setting_box">
                                    <div class="o_setting_left_pane"/>
                                    <div class="o_setting_right_pane">
                                        <div class="o_dashboard_engine_subsection_title">Chart Model</div>
                                        <div class="text-muted">
                                            Pick which records feed the chart — for example
                                            Pipeline or Sales Orders. The left button label
                                            and screen change with this choice so they
                                            always match. Leave empty for the dashboard default.
                                        </div>
                                        <div class="mt16">
                                            <field name="preferred_graph_variant_id" nolabel="1"
                                                   options="{'no_create': True, 'no_open': True}"
                                                   domain="[('blueprint_id', '=', blueprint_id)]"
                                                   placeholder="Dashboard default"/>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="row o_settings_container">
                                <div class="col-12 col-lg-6 o_setting_box">
                                    <div class="o_setting_left_pane"/>
                                    <div class="o_setting_right_pane">
                                        <div class="o_dashboard_engine_subsection_title">Group By</div>
                                        <div class="text-muted">
                                            Choose how the chart is split — for example
                                            by stage, salesperson, or team. Add more than
                                            one tag to go deeper (the first tag is the top
                                            level).
                                        </div>
                                        <div class="mt16">
                                            <field name="groupby_allowed_field_ids" invisible="1"/>
                                            <field name="groupby_ids" nolabel="1"
                                                   widget="dashboard_groupby_tags"
                                                   options="{'no_create': True}"
                                                   domain="[('id', 'in', groupby_allowed_field_ids)]"/>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-12 col-lg-6 o_setting_box">
                                    <div class="o_setting_left_pane"/>
                                    <div class="o_setting_right_pane">
                                        <div class="o_dashboard_engine_subsection_title">Measures</div>
                                        <div class="text-muted">
                                            Choose what number the chart shows — for
                                            example how many records, or expected revenue.
                                            Leave empty to count records.
                                        </div>
                                        <div class="mt16">
                                            <field name="measure_field_id" nolabel="1"
                                                   options="{'no_create': True}"
                                                   domain="[('model_id', '=', graph_model_id),
                                                            ('ttype', 'in', ['integer', 'float', 'monetary']),
                                                            ('store', '=', True)]"/>
                                            <field name="measure_aggregator"
                                                   invisible="not measure_field_id"/>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <!-- Title/help aligned like Custom Filter; Pipeline|Leads on their
                                 own full-width row so they get the same space as Group By|Measures. -->
                            <div class="row mt16 o_settings_container o_dashboard_engine_subsection"
                                 invisible="not has_include_scope">
                                <div class="col-12 o_setting_box">
                                    <div class="o_setting_left_pane"/>
                                    <div class="o_setting_right_pane">
                                        <div class="o_dashboard_engine_subsection_title">Data to Include</div>
                                        <div class="text-muted">
                                            Still under Graph Configuration. Tick which record
                                            types go into the chart — for example Pipeline,
                                            Leads, or both.
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="row o_settings_container o_dashboard_engine_include_scopes"
                                 invisible="not has_include_scope">
                                <div class="col-12">
                                    <field name="scope_ids" nolabel="1"
                                           widget="dashboard_scope_checkboxes"
                                           domain="[('id', 'in', applicable_include_scope_ids)]"/>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- 3) Filters: period + custom -->
                    <div class="o_setting_container">
                        <h4><b>Filters</b></h4>
                        <div class="o_dashboard_engine_setting_help">
                            Narrow data by date and custom rules. Date rows follow
                            the selected Chart Model (each model can offer different
                            dates). These filters apply to the chart, KPIs, bottom
                            buttons, views, and reports (including linked Sales when
                            available).
                        </div>
                        <div class="o_dashboard_engine_subsections">
                            <div class="row o_settings_container"
                                 invisible="not has_period_lines">
                                <div class="col-12 o_setting_box">
                                    <div class="o_setting_left_pane"/>
                                    <div class="o_setting_right_pane">
                                        Match
                                        <field name="period_operator"
                                               class="oe_inline text-primary fw-bold"
                                               nolabel="1"/>
                                        of the following rules:
                                    </div>
                                </div>
                            </div>
                            <div class="row mt16 o_settings_container"
                                 invisible="not has_period_lines">
                                <div class="col-12 o_setting_box">
                                    <div class="o_setting_left_pane"/>
                                    <div class="o_setting_right_pane">
                                        <field name="period_line_ids" nolabel="1"
                                               context="{'default_pref_id': id}">
                                            <list editable="bottom" create="false" delete="false">
                                                <field name="sequence" column_invisible="1"/>
                                                <field name="label" readonly="1" string="Date"/>
                                                <field name="period_mq_ids"
                                                       widget="many2many_tags"
                                                       options="{'no_create': True}"
                                                       string="Months"/>
                                                <field name="period_year_ids"
                                                       widget="many2many_tags"
                                                       options="{'no_create': True}"
                                                       string="Years"/>
                                                <field name="field_name" column_invisible="1"/>
                                                <field name="field_id" column_invisible="1"/>
                                            </list>
                                        </field>
                                    </div>
                                </div>
                            </div>
                            <div class="row mt16 o_settings_container o_dashboard_engine_subsection">
                                <div class="col-12 o_setting_box">
                                    <div class="o_setting_left_pane"/>
                                    <div class="o_setting_right_pane">
                                        <div class="o_dashboard_engine_subsection_title">Custom Filter</div>
                                        <div class="text-muted">
                                            Still under Filters. Add your own rule when months
                                            and years are not enough — for example only
                                            high-probability deals.
                                        </div>
                                        <div class="mt16">
                                            <field name="custom_filter" nolabel="1"
                                                   widget="domain"
                                                   options="{'model': 'graph_model'}"
                                                   invisible="not graph_model"/>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </sheet>
                <footer>
                    <button string="Apply" special="save" class="btn-primary"
                            data-hotkey="q"/>
                    <button string="Cancel" special="cancel" class="btn-secondary"
                            data-hotkey="x"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="view_dashboard_blueprint_form" model="ir.ui.view">
        <field name="name">dashboard.blueprint.form</field>
        <field name="model">dashboard.blueprint</field>
        <field name="arch" type="xml">
            <form string="Dashboard">
                <header>
                    <button name="action_open_studio" type="object"
                            string="Customize"
                            class="btn-primary"
                            groups="dashboard_engine.group_dashboard_engine_studio"/>
                    <button name="action_publish" type="object" string="Publish"
                            class="btn-primary" invisible="state == 'published'"/>
                    <button name="action_unpublish" type="object" string="Unpublish"
                            invisible="state != 'published'"/>
                    <button name="action_health_check" type="object"
                            string="Check References"/>
                    <button name="action_export_template" type="object"
                            string="Export as Template"
                            groups="dashboard_engine.group_dashboard_engine_manager"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,published"/>
                </header>
                <sheet>
                    <field name="active" invisible="1"/>
                    <widget name="web_ribbon" title="Archived"
                            bg_color="text-bg-danger" invisible="active"/>
                    <div class="alert alert-info" role="status"
                         invisible="state == 'published'">
                        <b>Advanced form</b> — for Administrators / power users.
                        Day-to-day building uses <b>Studio</b> (list → Customize, or New wizard).
                        Here you can still edit Configuration, Card layout, Manage Menu, and Technical.
                    </div>
                    <div class="alert alert-success" role="status"
                         invisible="state != 'published' or health_issue_count &gt; 0">
                        This dashboard is live. Open it from the menu you configured,
                        or use <b>Check References</b> after installing / removing apps.
                    </div>
                    <div class="alert alert-warning" role="alert"
                         invisible="health_issue_count == 0">
                        <field name="health_issue_count" invisible="1"/>
                        This dashboard points at things that are no longer installed:
                        <field name="health_message" nolabel="1" readonly="1"
                               class="mb-0"/>
                    </div>
                    <div class="oe_title">
                        <label for="name" string="Name"/>
                        <h1><field name="name" placeholder="e.g. CRM Customers"/></h1>
                    </div>
                    <group>
                        <group string="Dashboard">
                            <field name="host_model_id" options="{'no_create': True}"
                                   domain="[('transient', '=', False)]"
                                   help="Business object shown as each kanban card (e.g. Contact)."/>
                            <field name="host_model_name" invisible="1"/>
                            <field name="host_link_model_ids" invisible="1"/>
                            <field name="module_ids" widget="many2many_tags"
                                   options="{'no_create': True}"
                                   help="Dashboard stays hidden until these apps are installed."/>
                            <field name="key"
                                   help="Stable id used in generated views. Leave as suggested unless you know you need another."/>
                            <field name="sequence"/>
                            <field name="company_id" groups="base.group_multi_company"
                                   options="{'no_create': True}"
                                   help="Leave empty to share this blueprint with every company. Dashboard data still follows each model's company rules."/>
                            <field name="share_link_ids" widget="many2many_tags"
                                   options="{'no_create': True}"
                                   domain="[('id', '!=', id),
                                            ('host_model_id', '=', host_model_id)]"/>
                        </group>
                        <group string="Menu">
                            <field name="group_id" options="{'no_create_edit': False}"/>
                            <field name="menu_sequence"/>
                            <field name="menu_name" invisible="group_id"/>
                            <field name="menu_parent_id" options="{'no_create': True}"
                                      invisible="group_id"/>
                            <field name="menu_group_ids" widget="many2many_tags" options="{'no_create': True}"
                                      invisible="group_id"/>
                            <field name="menu_web_icon" invisible="group_id"/>
                            <field name="menu_web_icon_data" invisible="group_id"/>
                        </group>
                        <group string="Kanban Lens">
                            <field name="lens_my_enabled"/>
                            <field name="lens_my_default" invisible="not lens_my_enabled"/>
                            <field name="lens_my_label" invisible="not lens_my_enabled"
                                   required="lens_my_enabled"/>
                            <field name="lens_kpis_enabled"/>
                            <field name="lens_kpis_default" invisible="not lens_kpis_enabled"/>
                            <field name="lens_kpis_label" invisible="not lens_kpis_enabled"
                                   required="lens_kpis_enabled"/>
                            <field name="lens_attention_enabled"/>
                            <field name="lens_attention_default"
                                   invisible="not lens_attention_enabled"/>
                            <field name="lens_attention_label"
                                   invisible="not lens_attention_enabled"
                                   required="lens_attention_enabled"/>
                        </group>
                    </group>
                    <notebook>
                        <!-- ========== Page 1: live Configuration popup parity ========== -->
                        <page string="Configuration" name="configuration">
                            <p class="text-muted mb-3">
                                Defaults for the live Configuration popup.
                            </p>
                            <div class="o_dashboard_blueprint_section mb-3">
                                <h4><b>My Data · Graph Configuration · Filters</b></h4>
                                <div class="text-muted mb-2">
                                    Same three blocks as the live ⚙️ popup and Studio Configuration.
                                    <b>Restrict</b> scopes → <b>My Data</b> (e.g. only mine; use <code>uid</code>).
                                    <b>Include</b> scopes → Graph Configuration → <b>Data to Include</b> (chart only).
                                    Date fields and custom domain seed the <b>Filters</b> block.
                                </div>
                                <field name="scope_ids" nolabel="1">
                                    <list editable="bottom">
                                        <field name="sequence" widget="handle"/>
                                        <field name="graph_model" column_invisible="1"/>
                                        <field name="name" width="140px"/>
                                        <field name="description" optional="show" width="180px"/>
                                        <field name="mode" width="90px"/>
                                        <field name="domain" widget="domain"
                                               options="{'model': 'graph_model', 'in_dialog': True, 'foldable': True, 'allow_expressions': True}"/>
                                        <field name="default_on" string="Default" width="70px"/>
                                    </list>
                                    <form string="Data Scope">
                                        <group>
                                            <field name="name"/>
                                            <field name="description"/>
                                            <field name="mode"/>
                                            <field name="default_on"/>
                                            <field name="graph_model" invisible="1"/>
                                            <field name="domain" widget="domain"
                                                   options="{'model': 'graph_model', 'allow_expressions': True}"/>
                                        </group>
                                        <group string="Label Variants">
                                            <field name="label_ids" nolabel="1">
                                                <list editable="bottom">
                                                    <field name="sequence" widget="handle"/>
                                                    <field name="module_depends"
                                                           placeholder="sale,website_sale"/>
                                                    <field name="name"/>
                                                    <field name="description"/>
                                                </list>
                                            </field>
                                        </group>
                                        <group string="Target Maps (linked My / panel filters)"
                                               invisible="mode != 'restrict'">
                                            <div class="text-muted mb-2" colspan="2">
                                                Map this My tick onto other models (e.g. Sales Orders).
                                                End users never edit this — only builders / Studio.
                                                Missing fields are skipped safely (no crash).
                                            </div>
                                            <field name="target_ids" nolabel="1" colspan="2">
                                                <list editable="bottom">
                                                    <field name="sequence" widget="handle"/>
                                                    <field name="target_model_id"
                                                           options="{'no_create': True}"
                                                           domain="[('transient', '=', False)]"/>
                                                    <field name="target_model" column_invisible="1"/>
                                                    <field name="domain" widget="domain"
                                                           options="{'model': 'target_model', 'in_dialog': True, 'allow_expressions': True}"/>
                                                    <field name="period_field_id" optional="show"
                                                           options="{'no_create': True}"
                                                           domain="[('model_id', '=', target_model_id),
                                                                    ('ttype', 'in', ['date', 'datetime'])]"/>
                                                    <field name="period_closed_field_id" optional="hide"
                                                           options="{'no_create': True}"
                                                           domain="[('model_id', '=', target_model_id),
                                                                    ('ttype', 'in', ['date', 'datetime'])]"/>
                                                    <field name="period_field" column_invisible="1"/>
                                                    <field name="period_closed_field" column_invisible="1"/>
                                                    <field name="module_depends" optional="show"
                                                           placeholder="sale"/>
                                                    <field name="apply_kpi" optional="hide"/>
                                                    <field name="apply_bottom" optional="hide"/>
                                                    <field name="apply_views" optional="hide"/>
                                                    <field name="apply_reports" optional="hide"/>
                                                    <field name="apply_menu_new" optional="hide"/>
                                                </list>
                                            </field>
                                        </group>
                                    </form>
                                </field>
                                <field name="scope_warning"
                                       invisible="graph_variant_ids"
                                       placeholder="e.g. At least one of 'Pipeline' or 'Leads' must stay on."/>
                                <div class="text-muted mb-2" invisible="graph_variant_ids">
                                    Shown in the live ⚙️ popup when all Include scopes are unticked.
                                    Leave empty for no warning. With Chart Model Options, set Scope Warning
                                    on each option row instead.
                                </div>
                            </div>

                            <div class="o_dashboard_blueprint_section mb-3">
                                <div class="row">
                                    <div class="col-12 col-lg-6">
                                        <h4><b>Graph Configuration · Shape</b></h4>
                                        <div class="text-muted mb-2">
                                            Chart Model Options hold Group By / Measure / Data to Include ticks
                                            for each chart model (even when there is only one). End users can
                                            still override Group By / Measures in the gear.
                                        </div>
                                        <div class="o_dashboard_engine_subsection mb-3">
                                            <div class="o_dashboard_engine_subsection_title">Chart Model Options</div>
                                            <div class="text-muted mb-2">
                                                Model + link + primary button + Group By / Measure / Include ticks
                                                per row. Mark one Default (mirrors blueprint chart fields).
                                            </div>
                                            <field name="graph_variant_ids" nolabel="1"
                                                   context="{'default_blueprint_id': id}">
                                                <list editable="bottom">
                                                    <field name="sequence" widget="handle"/>
                                                    <field name="is_default" string="Default"/>
                                                    <field name="graph_model_id" options="{'no_create': True}"
                                                           domain="[('id', 'in', parent.host_link_model_ids)]"/>
                                                    <field name="graph_model" column_invisible="1"/>
                                                    <field name="graph_data_field"
                                                             widget="dashboard_relation_path"
                                                             options="{'model': 'graph_model', 'follow_relations': True}"
                                                             placeholder="Drill into related records until this card’s model"/>
                                                    <field name="primary_button_label"/>
                                                    <field name="primary_action_id" options="{'no_create': True}"
                                                           domain="['|', ('res_model', '=', graph_model),
                                                                    ('res_model', '=', False)]"/>
                                                    <field name="primary_action_xmlid" column_invisible="1"/>
                                                    <field name="primary_action_context" optional="hide"/>
                                                    <field name="graph_groupby_allowed_field_ids" column_invisible="1"/>
                                                    <field name="applicable_include_scope_ids" column_invisible="1"/>
                                                    <field name="default_ordered_groupby_ids" column_invisible="1"/>
                                                    <field name="default_groupby_ids"
                                                           widget="dashboard_groupby_tags"
                                                           options="{'no_create': True}"
                                                           domain="[('id', 'in', graph_groupby_allowed_field_ids)]"
                                                           string="Group By"/>
                                                    <field name="default_measure_field_id"
                                                           options="{'no_create': True}"
                                                           domain="[('model_id', '=', graph_model_id),
                                                                    ('ttype', 'in', ['integer', 'float', 'monetary']),
                                                                    ('store', '=', True)]"
                                                           string="Measure"/>
                                                    <field name="default_measure_aggregator"
                                                           string="Measured As"/>
                                                    <field name="default_scope_ids"
                                                           widget="many2many_tags"
                                                           options="{'no_create': True}"
                                                           domain="[('id', 'in', applicable_include_scope_ids)]"
                                                           string="Data to Include"/>
                                                    <field name="scope_warning"
                                                           string="Scope Warning"
                                                           optional="show"
                                                           placeholder="Warning if all Include scopes off"/>
                                                </list>
                                                <form string="Chart Model Option">
                                                    <group>
                                                        <field name="is_default"/>
                                                        <field name="graph_model_id" options="{'no_create': True}"
                                                               domain="[('id', 'in', parent.host_link_model_ids)]"/>
                                                        <field name="graph_model" invisible="1"/>
                                                        <field name="graph_data_field"
                                                                 widget="dashboard_relation_path"
                                                                 options="{'model': 'graph_model', 'follow_relations': True}"/>
                                                        <field name="primary_button_label"/>
                                                        <field name="primary_action_id" options="{'no_create': True}"/>
                                                        <field name="scope_warning"/>
                                                    </group>
                                                    <group string="Date Filters">
                                                        <field name="date_filter_ids" nolabel="1"
                                                               context="{'default_variant_id': id}">
                                                            <list editable="bottom">
                                                                <field name="sequence" widget="handle"/>
                                                                <field name="label"/>
                                                                <field name="field_id"
                                                                       options="{'no_create': True}"
                                                                       domain="[('model_id', '=', parent.graph_model_id),
                                                                                ('ttype', 'in', ['date', 'datetime'])]"/>
                                                                <field name="field_name" column_invisible="1"/>
                                                                <field name="default_period_mq_ids"
                                                                       widget="many2many_tags"
                                                                       options="{'no_create': True}"
                                                                       optional="show"/>
                                                                <field name="default_period_year_ids"
                                                                       widget="many2many_tags"
                                                                       options="{'no_create': True}"
                                                                       optional="show"/>
                                                            </list>
                                                        </field>
                                                    </group>
                                                    <group string="Custom Filter">
                                                        <field name="graph_domain" widget="domain"
                                                               options="{'model': 'graph_model', 'in_dialog': True}"/>
                                                    </group>
                                                </form>
                                            </field>
                                        </div>
                                        <group col="1">
                                            <field name="graph_model_id"
                                                   options="{'no_create': True}"
                                                   domain="[('id', 'in', host_link_model_ids)]"
                                                   invisible="graph_variant_ids"
                                                   help="Used when there are no picker options. Prefer adding a Chart Model Option."/>
                                            <field name="graph_model" invisible="1"/>
                                            <field name="graph_data_field"
                                                   widget="dashboard_relation_path"
                                                   options="{'model': 'graph_model', 'follow_relations': True}"
                                                   invisible="not graph_model_id or graph_variant_ids"
                                                   placeholder="Drill into related records until this card’s model"/>
                                            <field name="graph_data_field_id" invisible="1"/>
                                            <field name="graph_relation_path_id" invisible="1"/>
                                            <field name="include_child_records"
                                                   help="Roll child cards into parent totals when the host model has a parent."/>
                                            <field name="graph_caption"/>
                                            <field name="ordered_graph_groupby_ids" invisible="1"/>
                                            <field name="graph_groupby_allowed_field_ids" invisible="1"/>
                                            <field name="graph_groupby_ids"
                                                   widget="dashboard_groupby_tags"
                                                   options="{'no_create': True}"
                                                   invisible="not graph_model_id or graph_variant_ids"
                                                   domain="[('id', 'in', graph_groupby_allowed_field_ids)]"/>
                                            <field name="graph_measure_field_id"
                                                   options="{'no_create': True}"
                                                   invisible="not graph_model_id or graph_variant_ids"
                                                   domain="[('model_id', '=', graph_model_id),
                                                            ('ttype', 'in', ['integer', 'float', 'monetary']),
                                                            ('store', '=', True)]"
                                                   help="Leave empty to count records. Hidden when Chart Model Options exist."/>
                                            <field name="graph_measure_aggregator"
                                                   invisible="not graph_measure_field_id or graph_variant_ids"/>
                                        </group>
                                    </div>
                                    <div class="col-12 col-lg-6">
                                        <h4><b>Fallback seeds</b></h4>
                                        <div class="text-muted mb-2">
                                            Used only to seed empty Chart Model Option Date Filters / Custom Filter
                                            when the option model matches. Prefer editing Filters and Custom Filter
                                            on each Chart Model Option (expand a row in Studio).
                                        </div>
                                        <group col="1">
                                            <field name="period_field_id"
                                                   options="{'no_create': True}"
                                                   invisible="not graph_model_id"
                                                   domain="[('model_id', '=', graph_model_id),
                                                            ('ttype', 'in', ['date', 'datetime'])]"/>
                                            <field name="closed_period_field_id"
                                                   options="{'no_create': True}"
                                                   invisible="not graph_model_id"
                                                   domain="[('model_id', '=', graph_model_id),
                                                            ('ttype', 'in', ['date', 'datetime'])]"/>
                                            <field name="graph_domain"
                                                   widget="domain"
                                                   options="{'model': 'graph_model', 'in_dialog': True}"
                                                   invisible="not graph_model_id"
                                                   groups="dashboard_engine.group_dashboard_engine_manager"/>
                                        </group>
                                    </div>
                                </div>
                            </div>
                        </page>

                        <!-- ========== Page 2: live card anatomy ========== -->
                        <page string="Card Layout" name="kanban_card">
                            <div class="o_dashboard_blueprint_card_map mb-3" aria-hidden="true">
                                <div class="o_db_map_zone">Header</div>
                                <div class="o_db_map_row">
                                    <div class="o_db_map_zone">Primary Button</div>
                                    <div class="o_db_map_zone">Right · KPIs</div>
                                </div>
                                <div class="o_db_map_zone">Footer · Totals</div>
                                <div class="o_db_map_zone">Footer · Shortcuts</div>
                            </div>
                            <p class="text-muted mb-3">
                                Zones below match this map top → bottom.
                            </p>
                            <separator string="Header"/>
                            <p class="text-muted">
                                Title, image, and header lines on the card face.
                            </p>
                            <group>
                                <group string="Title &amp; Image">
                                    <field name="header_title_field_id"
                                           options="{'no_create': True}"
                                           domain="[('model', '=', host_model_name),
                                                    ('ttype', 'not in', ['binary', 'html', 'one2many', 'many2many'])]"/>
                                    <field name="header_image_field_id"
                                           options="{'no_create': True}"
                                           domain="[('model', '=', host_model_name),
                                                    ('ttype', '=', 'binary')]"/>
                                    <field name="header_image_style"
                                           invisible="not header_image_field_id"/>
                                </group>
                                <group string="Header Lines">
                                    <field name="header_line_ids" nolabel="1" colspan="2">
                                        <list editable="bottom">
                                            <field name="sequence" widget="handle"/>
                                            <field name="host_model_name" column_invisible="1"/>
                                            <field name="ordered_field_ids" column_invisible="1"/>
                                            <field name="has_multiple_fields" column_invisible="1"/>
                                            <field name="kind"/>
                                            <field name="alignment"/>
                                            <field name="icon"
                                                   invisible="kind != 'inline'"/>
                                            <field name="field_ids"
                                                   widget="many2many_ordered_tags"
                                                   options="{'no_create': True, 'separator': False, 'label_format': 'string_name'}"
                                                   domain="[('model', '=', host_model_name)]"/>
                                            <field name="separator"
                                                   invisible="not has_multiple_fields"/>
                                        </list>
                                    </field>
                                </group>
                            </group>

                            <separator string="Primary Button"/>
                            <p class="text-muted">
                                The button beside the graph; chart defaults are on Configuration.
                            </p>

                            <group>
                                <group string="Button Label">
                                    <field name="primary_button_label"/>
                                    <div class="text-muted" colspan="2">
                                        Optional: different label when a Configuration
                                        filter is off (e.g. Pipeline vs Leads).
                                    </div>
                                    <field name="primary_label_alt_scope_id"
                                           options="{'no_create': True}"/>
                                    <field name="primary_label_alt"
                                           invisible="not primary_label_alt_scope_id"
                                           placeholder="e.g. Leads Analysis"/>
                                </group>
                                <group string="Action"
                                       groups="dashboard_engine.group_dashboard_engine_manager">
                                    <field name="graph_model" invisible="1"/>
                                    <field name="primary_action_id"
                                           options="{'no_create': True}"
                                           domain="['|', '|', ('res_model', '=', host_model_name),
                                                    ('res_model', '=', graph_model),
                                                    ('res_model', '=', False)]"/>
                                    <div class="text-muted" colspan="2">
                                        Leave empty to build the screen from the chart
                                        settings. Rows below: if apps are installed,
                                        open that action instead (first match wins).
                                    </div>
                                    <field name="alternate_action_ids" nolabel="1" colspan="2">
                                        <list editable="bottom">
                                            <field name="sequence" widget="handle"/>
                                            <field name="name" string="Label"/>
                                            <field name="host_model_name" column_invisible="1"/>
                                            <field name="graph_model" column_invisible="1"/>
                                            <field name="module_ids" string="Required Apps"
                                                   widget="many2many_tags"
                                                   options="{'no_create': True}"/>
                                            <field name="action_id"
                                                   options="{'no_create': True}"
                                                   domain="['|', '|', ('res_model', '=', host_model_name),
                                                            ('res_model', '=', graph_model),
                                                            ('res_model', '=', False)]"/>
                                        </list>
                                    </field>
                                </group>
                                <group string="Action"
                                       groups="!dashboard_engine.group_dashboard_engine_manager">
                                    <field name="primary_action_id"
                                           options="{'no_create': True}"
                                           domain="['|', '|', ('res_model', '=', host_model_name),
                                                    ('res_model', '=', graph_model),
                                                    ('res_model', '=', False)]"/>
                                    <div class="text-muted" colspan="2">
                                        Leave empty to build the screen from the chart
                                        settings automatically.
                                    </div>
                                </group>
                            </group>

                            <group col="1"
                                   groups="dashboard_engine.group_dashboard_engine_manager">
                                <div class="text-muted" colspan="2">
                                    On customer Acme, <code>{{id}}</code> becomes Acme’s id so the opened
                                    list is filtered to that customer.
                                </div>
                                <field name="graph_model" invisible="1"/>
                                <field name="primary_action_domain"
                                       widget="domain"
                                       options="{'model': 'graph_model', 'in_dialog': False, 'foldable': False, 'allow_expressions': True}"
                                       invisible="not graph_model"/>
                                <field name="primary_action_context"
                                       widget="dashboard_context_kv"
                                       string="Action Defaults"/>
                            </group>

                            <separator string="Right · KPIs"/>
                            <p class="text-muted">
                                KPI links beside the primary button; Shows picks count, amount, or both.
                            </p>
                            <field name="kpi_slot_ids" nolabel="1"
                                   context="{'default_section': 'kpi', 'default_blueprint_id': id}">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="section" column_invisible="1"/>
                                    <field name="host_model_name" column_invisible="1"/>
                                    <field name="compute_model" column_invisible="1"/>
                                    <field name="action_model" column_invisible="1"/>
                                    <field name="value_mode"/>
                                    <field name="label"/>
                                    <field name="amount_measure_field_id"
                                           invisible="value_mode == 'count'"
                                           required="value_mode in ('amount', 'count_amount')"
                                           options="{'no_create': True}"
                                           domain="[('model_id', '=', compute_model_id),
                                                    ('ttype', 'in', ['integer', 'float', 'monetary']),
                                                    ('store', '=', True)]"/>
                                    <field name="compute_model_id"
                                           options="{'no_create': True}"
                                           domain="[('id', 'in', parent.host_link_model_ids)]"/>
                                    <field name="relate_field"
                                           widget="dashboard_relation_path"
                                           options="{'model': 'compute_model', 'follow_relations': True}"
                                           placeholder="Link to Host (or leave empty)"/>
                                    <field name="relate_field_id" column_invisible="1"/>
                                    <field name="relation_path_id" column_invisible="1"/>
                                    <field name="condition_ids" optional="show"
                                           widget="many2many_tags"
                                           options="{'no_create_edit': False}"
                                           domain="[('model', '=', compute_model)]"/>
                                    <field name="action_id"
                                           options="{'no_create': True}"
                                           domain="['|', '|', '|',
                                                    ('res_model', '=', action_model),
                                                    ('res_model', '=', compute_model),
                                                    ('res_model', '=', host_model_name),
                                                    ('res_model', '=', False)]"/>
                                    <field name="action_context"
                                           widget="dashboard_context_kv"
                                           string="Action Defaults"
                                           optional="hide"/>
                                    <field name="name" string="Technical Name" optional="hide"/>
                                    <field name="label_plural" optional="hide"/>
                                    <field name="style" optional="hide"/>
                                    <field name="style_mode" optional="hide"/>
                                    <field name="is_attention_signal" optional="hide"
                                           string="Attention"/>
                                    <field name="panel_follow_my" optional="hide"
                                           string="Follow My"/>
                                    <field name="panel_follow_filters" optional="hide"
                                           string="Follow Filters"/>
                                    <field name="show_if_zero" optional="hide"/>
                                    <field name="module_ids" optional="hide"
                                           widget="many2many_tags"
                                           options="{'no_create': True}"/>
                                </list>
                            </field>

                            <separator string="Footer · Totals"/>
                            <p class="text-muted">
                                Same as KPIs: Source Model + filters, or clear Source Model and use Count/Amount Field on the card. Always Count + Amount.
                            </p>
                            <field name="button_box_slot_ids" nolabel="1"
                                   context="{'default_section': 'button_box', 'default_blueprint_id': id, 'default_value_mode': 'count_amount'}">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="section" column_invisible="1"/>
                                    <field name="host_model_name" column_invisible="1"/>
                                    <field name="compute_model" column_invisible="1"/>
                                    <field name="action_model" column_invisible="1"/>
                                    <field name="ui_number_from_related" column_invisible="1"/>
                                    <field name="ui_number_from_host" column_invisible="1"/>
                                    <field name="value_mode" column_invisible="1"/>
                                    <field name="label"/>
                                    <field name="label_plural" optional="hide"/>
                                    <field name="icon"/>
                                    <field name="amount_measure_field_id"
                                           invisible="not ui_number_from_related"
                                           options="{'no_create': True}"
                                           domain="[('model_id', '=', compute_model_id),
                                                    ('ttype', 'in', ['integer', 'float', 'monetary']),
                                                    ('store', '=', True)]"/>
                                    <field name="compute_model_id"
                                           invisible="not ui_number_from_related"
                                           options="{'no_create': True}"
                                           domain="[('id', 'in', parent.host_link_model_ids)]"/>
                                    <field name="relate_field"
                                           invisible="not ui_number_from_related"
                                           widget="dashboard_relation_path"
                                           options="{'model': 'compute_model', 'follow_relations': True}"
                                           placeholder="Link to Host (or leave empty)"/>
                                    <field name="relate_field_id" column_invisible="1"/>
                                    <field name="relation_path_id" column_invisible="1"/>
                                    <field name="count_field_id"
                                           invisible="not ui_number_from_host"
                                           options="{'no_create': True}"
                                           domain="[('model', '=', host_model_name),
                                                    ('ttype', 'in', ['integer', 'float', 'monetary'])]"/>
                                    <field name="amount_field_id"
                                           invisible="not ui_number_from_host"
                                           options="{'no_create': True}"
                                           domain="[('model', '=', host_model_name),
                                                    ('ttype', 'in', ['integer', 'float', 'monetary'])]"/>
                                    <field name="condition_ids" optional="show"
                                           widget="many2many_tags"
                                           options="{'no_create_edit': False}"
                                           domain="[('model', '=', compute_model)]"/>
                                    <field name="action_id"
                                           options="{'no_create': True}"
                                           domain="['|', '|', '|',
                                                    ('res_model', '=', action_model),
                                                    ('res_model', '=', compute_model),
                                                    ('res_model', '=', host_model_name),
                                                    ('res_model', '=', False)]"/>
                                    <field name="action_context"
                                           widget="dashboard_context_kv"
                                           string="Action Defaults"
                                           optional="hide"/>
                                    <field name="name" string="Technical Name" optional="hide"/>
                                    <field name="style" optional="hide"/>
                                    <field name="style_mode" optional="hide"/>
                                    <field name="is_attention_signal" optional="hide"
                                           string="Attention"/>
                                    <field name="panel_follow_my" optional="hide"
                                           string="Follow My"/>
                                    <field name="panel_follow_filters" optional="hide"
                                           string="Follow Filters"/>
                                    <field name="show_if_zero" optional="hide"/>
                                    <field name="action_method" optional="hide"/>
                                    <field name="module_ids" optional="hide"
                                           widget="many2many_tags"
                                           options="{'no_create': True}"/>
                                </list>
                            </field>

                            <separator string="Footer · Shortcuts"/>
                            <p class="text-muted">
                                Same as KPIs: Source Model + filters, or clear Source Model and use Count/Amount Field on the card. Always Count + Amount.
                            </p>
                            <field name="bottom_slot_ids" nolabel="1"
                                   context="{'default_section': 'bottom', 'default_blueprint_id': id, 'default_value_mode': 'count_amount'}">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="section" column_invisible="1"/>
                                    <field name="host_model_name" column_invisible="1"/>
                                    <field name="compute_model" column_invisible="1"/>
                                    <field name="action_model" column_invisible="1"/>
                                    <field name="ui_number_from_related" column_invisible="1"/>
                                    <field name="ui_number_from_host" column_invisible="1"/>
                                    <field name="value_mode" column_invisible="1"/>
                                    <field name="label"/>
                                    <field name="label_plural" optional="hide"/>
                                    <field name="icon"/>
                                    <field name="amount_measure_field_id"
                                           invisible="not ui_number_from_related"
                                           options="{'no_create': True}"
                                           domain="[('model_id', '=', compute_model_id),
                                                    ('ttype', 'in', ['integer', 'float', 'monetary']),
                                                    ('store', '=', True)]"/>
                                    <field name="compute_model_id"
                                           invisible="not ui_number_from_related"
                                           options="{'no_create': True}"
                                           domain="[('id', 'in', parent.host_link_model_ids)]"/>
                                    <field name="relate_field"
                                           invisible="not ui_number_from_related"
                                           widget="dashboard_relation_path"
                                           options="{'model': 'compute_model', 'follow_relations': True}"
                                           placeholder="Link to Host (or leave empty)"/>
                                    <field name="relate_field_id" column_invisible="1"/>
                                    <field name="relation_path_id" column_invisible="1"/>
                                    <field name="count_field_id"
                                           invisible="not ui_number_from_host"
                                           options="{'no_create': True}"
                                           domain="[('model', '=', host_model_name),
                                                    ('ttype', 'in', ['integer', 'float', 'monetary'])]"/>
                                    <field name="amount_field_id"
                                           invisible="not ui_number_from_host"
                                           options="{'no_create': True}"
                                           domain="[('model', '=', host_model_name),
                                                    ('ttype', 'in', ['integer', 'float', 'monetary'])]"/>
                                    <field name="condition_ids" optional="show"
                                           widget="many2many_tags"
                                           options="{'no_create_edit': False}"
                                           domain="[('model', '=', compute_model)]"/>
                                    <field name="action_id"
                                           options="{'no_create': True}"
                                           domain="['|', '|', '|',
                                                    ('res_model', '=', action_model),
                                                    ('res_model', '=', compute_model),
                                                    ('res_model', '=', host_model_name),
                                                    ('res_model', '=', False)]"/>
                                    <field name="action_context"
                                           widget="dashboard_context_kv"
                                           string="Action Defaults"
                                           optional="hide"/>
                                    <field name="name" string="Technical Name" optional="hide"/>
                                    <field name="show_if_zero" optional="hide"/>
                                    <field name="module_ids" optional="hide"
                                           widget="many2many_tags"
                                           options="{'no_create': True}"/>
                                </list>
                            </field>

                        </page>

                        <page string="Manage Menu" name="manage_menu">
                            <p class="text-muted">
                                Same as the live ⋮ menu — links only.
                            </p>

                            <separator string="Views"/>
                            <field name="menu_views_slot_ids" nolabel="1"
                                   context="{'default_section': 'menu_views', 'default_blueprint_id': id}">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="section" column_invisible="1"/>
                                    <field name="host_model_name" column_invisible="1"/>
                                    <field name="action_model" column_invisible="1"/>
                                    <field name="compute_model" column_invisible="1"/>
                                    <field name="label"/>
                                    <field name="action_id"
                                           options="{'no_create': True}"
                                           domain="['|', '|', '|',
                                                    ('res_model', '=', action_model),
                                                    ('res_model', '=', compute_model),
                                                    ('res_model', '=', host_model_name),
                                                    ('res_model', '=', False)]"/>
                                    <field name="action_context"
                                           widget="dashboard_context_kv"
                                           string="Action Defaults"
                                           optional="hide"/>
                                    <field name="name" string="Technical Name" optional="hide"/>
                                    <field name="module_ids" optional="hide"
                                           widget="many2many_tags"
                                           options="{'no_create': True}"/>
                                    <field name="group_ids" optional="hide"
                                           widget="many2many_tags"
                                           options="{'no_create': True}"/>
                                </list>
                            </field>

                            <separator string="New"/>
                            <field name="menu_new_slot_ids" nolabel="1"
                                   context="{'default_section': 'menu_new', 'default_blueprint_id': id}">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="section" column_invisible="1"/>
                                    <field name="host_model_name" column_invisible="1"/>
                                    <field name="action_model" column_invisible="1"/>
                                    <field name="compute_model" column_invisible="1"/>
                                    <field name="label"/>
                                    <field name="action_id"
                                           options="{'no_create': True}"
                                           domain="['|', '|', '|',
                                                    ('res_model', '=', action_model),
                                                    ('res_model', '=', compute_model),
                                                    ('res_model', '=', host_model_name),
                                                    ('res_model', '=', False)]"/>
                                    <field name="action_context"
                                           widget="dashboard_context_kv"
                                           string="Action Defaults"
                                           optional="hide"/>
                                    <field name="name" string="Technical Name" optional="hide"/>
                                    <field name="module_ids" optional="hide"
                                           widget="many2many_tags"
                                           options="{'no_create': True}"/>
                                    <field name="group_ids" optional="hide"
                                           widget="many2many_tags"
                                           options="{'no_create': True}"/>
                                </list>
                            </field>

                            <separator string="Reports"/>
                            <field name="menu_reports_slot_ids" nolabel="1"
                                   context="{'default_section': 'menu_reports', 'default_blueprint_id': id}">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="section" column_invisible="1"/>
                                    <field name="host_model_name" column_invisible="1"/>
                                    <field name="action_model" column_invisible="1"/>
                                    <field name="compute_model" column_invisible="1"/>
                                    <field name="label"/>
                                    <field name="action_id"
                                           options="{'no_create': True}"
                                           domain="['|', '|', '|',
                                                    ('res_model', '=', action_model),
                                                    ('res_model', '=', compute_model),
                                                    ('res_model', '=', host_model_name),
                                                    ('res_model', '=', False)]"/>
                                    <field name="action_context"
                                           widget="dashboard_context_kv"
                                           string="Action Defaults"
                                           optional="hide"/>
                                    <field name="name" string="Technical Name" optional="hide"/>
                                    <field name="module_ids" optional="hide"
                                           widget="many2many_tags"
                                           options="{'no_create': True}"/>
                                    <field name="group_ids" optional="hide"
                                           widget="many2many_tags"
                                           options="{'no_create': True}"/>
                                </list>
                            </field>
                        </page>

                        <page string="Technical" name="technical"
                              groups="dashboard_engine.group_dashboard_engine_manager">
                            <group>
                                <field name="generated_view_id"/>
                                <field name="generated_action_id"/>
                                <field name="generated_menu_id"/>
                            </group>
                            <group string="Stored Field Names">
                                <div class="text-muted" colspan="2">
                                    Written automatically from the pickers. These are
                                    what the dashboard runs on, what an export carries
                                    to another database, and what survives uninstalling
                                    the app a blueprint points at.
                                </div>
                                <field name="graph_model" readonly="1"/>
                                <field name="graph_data_field" readonly="1"/>
                                <field name="graph_groupby" readonly="1"/>
                                <field name="graph_measure" readonly="1"/>
                                <field name="module_depends" readonly="1"/>
                                <field name="menu_parent_xmlid" readonly="1"/>
                                <field name="primary_action_xmlid" readonly="1"/>
                                <field name="header_title_field" readonly="1"/>
                                <field name="header_image_field" readonly="1"/>
                            </group>
                            <group string="All Links">
                                <field name="slot_ids" nolabel="1">
                                    <list editable="bottom">
                                        <field name="sequence" widget="handle"/>
                                        <field name="section"/>
                                        <field name="key"/>
                                        <field name="name"/>
                                        <field name="visible_if_context"/>
                                        <field name="action_context"
                                               widget="dashboard_context_kv"
                                               string="Action Defaults"
                                               optional="show"/>
                                        <field name="action_view_mode"/>
                                        <field name="action_method" optional="show"/>
                                        <field name="host_model_name" column_invisible="1"/>
                                        <field name="count_field_id"
                                               options="{'no_create': True}"
                                               domain="[('model', '=', host_model_name),
                                                        ('ttype', 'in', ['integer', 'float', 'monetary'])]"/>
                                        <field name="amount_field_id"
                                               options="{'no_create': True}"
                                               domain="[('model', '=', host_model_name),
                                                        ('ttype', 'in', ['integer', 'float', 'monetary'])]"/>
                                    </list>
                                </field>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_dashboard_blueprint" model="ir.actions.act_window">
        <field name="name">Dashboards</field>
        <field name="res_model">dashboard.blueprint</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_dashboard_blueprint_search"/>
        <field name="context">{'search_default_published': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a dashboard for any Odoo model.
            </p>
            <p>
                Click <b>New</b> to open the create wizard, then edit in
                <b>Studio</b>. Click a row or <b>Customize</b> to reopen Studio.
                Administrators can open the classic <b>Advanced</b> form when needed.
            </p>
        </field>
    </record>
</odoo>
